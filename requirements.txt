# Vabamorf: morphology and synthesis. Pinned exactly — it is the answer key;
# measure synthesised forms before changing the version.
estnltk==1.7.5
fastapi>=0.115
uvicorn[standard]>=0.30
fsrs>=6.0                # FSRS-6 spaced repetition (MIT)

# Test-only: Starlette's TestClient transport, and reading workflow YAML.
httpx2
PyYAML>=6.0
